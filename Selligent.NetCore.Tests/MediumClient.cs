using Xunit;

namespace Selligent.NetCore.Tests
{
  public class MediumClient_Tests
  {

    /*
    public MediumClient GetClient()
    {
      return new MediumClient(new ClientConfiguration {

      });
    }

    [Fact]
    public async void GetMediumsAsync_Test()
    {
      var client = GetClient();
      Assert.NotNull(client);
      var results = await client.GetMediumsAsync();
      Assert.NotEmpty(results.Mediums1);
    }*/
  }
}
